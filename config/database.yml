Default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: rs_server
  user: postgres

production:
  url: <%= ENV['DATABASE_URL'] %>